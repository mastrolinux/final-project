# Development and CI tool dependencies
# Usage: pip install -r requirements-dev.txt
#
# Includes all production dependencies plus type checking and linting tools.

-r requirements.txt

# Type checking
mypy>=1.8.0

# Type stubs (reduce ignore-missing-imports noise)
types-redis>=4.6.0

# Linting and formatting
ruff>=0.9.0

# Security scanning
bandit>=1.7.0
