# Production Environment Configuration
# Use this as a template for production environment variables
# Set these values in your deployment platform (Render.com, etc.)
# NEVER commit actual production values to version control

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
APP_NAME="Identity and Profile Management API"
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO

# =============================================================================
# DATABASE CONFIGURATION - PRODUCTION SUPABASE
# =============================================================================
# Get from: Supabase Dashboard > Settings > Database
# Use the "Connection pooling" URL for production (uses PgBouncer)
# Format: postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:6543/postgres?pgbouncer=true

DATABASE_POOL_SIZE=10
DATABASE_MAX_OVERFLOW=20

# =============================================================================
# SUPABASE CONFIGURATION - PRODUCTION
# =============================================================================
# Get from: Supabase Dashboard > Settings > API
# Project URL format: https://[PROJECT-REF].supabase.co
SUPABASE_URL=https://[PROJECT-REF].supabase.co

# Public URL for browser-resolvable Supabase access (e.g., avatar uploads)
# Typically same as SUPABASE_URL in production
SUPABASE_PUBLIC_URL=https://[PROJECT-REF].supabase.co

# Anon Key (public key - safe to expose)
# Get from: Supabase Dashboard > Settings > API > Project API keys > anon public
SUPABASE_ANON_KEY=[YOUR-ANON-KEY-FROM-SUPABASE-DASHBOARD]

# Service Role Key (SECRET - never expose to clients)
# Get from: Supabase Dashboard > Settings > API > Project API keys > service_role secret
SUPABASE_SERVICE_KEY=[YOUR-SERVICE-ROLE-KEY-FROM-SUPABASE-DASHBOARD]

# JWT Secret
# Get from: Supabase Dashboard > Settings > API > JWT Settings > JWT Secret
SUPABASE_JWT_SECRET=[YOUR-JWT-SECRET-FROM-SUPABASE-DASHBOARD]

# =============================================================================
# SECURITY CONFIGURATION - PRODUCTION
# =============================================================================
# CRITICAL: Generate a strong secret key
# Generate with: openssl rand -hex 32
# Store securely in deployment platform environment variables
SECRET_KEY=[GENERATE-WITH-OPENSSL-RAND-HEX-32]

ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# JWT Authentication (AuthService) - separate from SECRET_KEY
# Generate with: openssl rand -hex 32
JWT_SECRET_KEY=[GENERATE-WITH-OPENSSL-RAND-HEX-32]

# =============================================================================
# CORS CONFIGURATION - PRODUCTION
# =============================================================================
# Comma-separated list of allowed origins
# Include your production frontend domains
# Example: https://app.yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS_STR=https://app.yourdomain.com,https://www.yourdomain.com

# =============================================================================
# SMTP CONFIGURATION - PRODUCTION (Mailgun)
# =============================================================================
# Get credentials from Mailgun Dashboard > Sending > Domain settings > SMTP credentials
# US region: smtp.mailgun.org | EU region: smtp.eu.mailgun.org
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_FROM_EMAIL=noreply@yourdomain.com
SMTP_FROM_NAME=Identity Management System
SMTP_USER=postmaster@yourdomain.mailgun.org
SMTP_PASSWORD=[YOUR-MAILGUN-SMTP-PASSWORD]
SMTP_USE_TLS=true

# Frontend and API URLs for email links
FRONTEND_URL=https://app.yourdomain.com
API_BASE_URL=https://api.yourdomain.com

# =============================================================================
# OAUTH 2.0 SOCIAL LOGIN - PRODUCTION
# =============================================================================
# Google OAuth 2.0 Credentials
# Get from: Google Cloud Console > APIs & Services > Credentials
# Update redirect URI to production domain
GOOGLE_CLIENT_ID=[YOUR-GOOGLE-CLIENT-ID].apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=[YOUR-GOOGLE-CLIENT-SECRET]
GOOGLE_REDIRECT_URI=https://identity-api.onrender.com/api/v1/auth/social/google/callback

# =============================================================================
# ADMIN CONFIGURATION
# =============================================================================
# Comma-separated list of admin user emails (bootstrap admins)
# These users have admin access even if is_admin=false in DB
ADMIN_USER_EMAILS=admin@yourdomain.com

# =============================================================================
# PRIVACY CONFIGURATION
# =============================================================================
# Soft-delete retention period in days (GDPR-inspired)
# Accounts marked for deletion are recoverable within this window
DELETION_RETENTION_DAYS=30

# =============================================================================
# RATE LIMITING - PRODUCTION (recommended)
# =============================================================================
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=60

# =============================================================================
# REDIS CONFIGURATION - PRODUCTION (required)
# =============================================================================
# Required for JWT refresh token blacklist (fail-closed security) and
# Celery async email tasks. On Render, use the fromService connection string.
# Starter plan recommended for persistence.
REDIS_ENABLED=true
REDIS_URL=redis://:[PASSWORD]@[HOST]:[PORT]/0
REDIS_TTL_SECONDS=300

# =============================================================================
# OBSERVABILITY - PRODUCTION
# =============================================================================
# Sentry for error tracking (recommended)
# Get from: https://sentry.io/
SENTRY_DSN=https://[KEY]@[ORG].ingest.sentry.io/[PROJECT]

# Application Performance Monitoring
APM_ENABLED=true
APM_SERVICE_NAME=identity-api-production

# =============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# =============================================================================
# [ ] Create Supabase production project and apply migrations (supabase db push)
# [ ] Generate strong SECRET_KEY and JWT_SECRET_KEY (openssl rand -hex 32)
#     Note: render.yaml uses generateValue: true to auto-generate these
# [ ] Set all Supabase credentials from dashboard
# [ ] Set SUPABASE_PUBLIC_URL for browser-resolvable avatar uploads
# [ ] Update ALLOWED_ORIGINS_STR with production frontend domain
# [ ] Configure Mailgun SMTP credentials (host, user, password)
# [ ] Update SMTP_FROM_EMAIL with verified Mailgun domain
# [ ] Set FRONTEND_URL and API_BASE_URL for email links
# [ ] Configure Google OAuth redirect URI for production domain
# [ ] Set ADMIN_USER_EMAILS for bootstrap admin access
# [ ] Verify Redis connection (required for token blacklist and Celery)
# [ ] Enable rate limiting
# [ ] Configure Sentry for error tracking (optional)
# [ ] Test all environment variables are set (app fails fast if missing)
# [ ] Verify database connection pooling is enabled
# [ ] Ensure DEBUG=false and LOG_LEVEL=INFO

